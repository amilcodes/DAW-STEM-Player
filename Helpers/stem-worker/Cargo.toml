[package]
name = "stem-worker"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
serde_json = "1"
stem-splitter-core = { version = "1.2.0", default-features = false, features = ["coreml", "xnnpack"] }

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
